#include <bits/stdc++.h>
#define ll long long
using namespace std;

const int half=131072;
ll tree[half*2][2];
int N,Q,R;
ll mod;



int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>N>>Q;
    for(int i=0;i^Q;++i){
        cin>>R;
        if(R){

        }
    }

    return 0;
}