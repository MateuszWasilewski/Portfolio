#include <bits/stdc++.h>

using namespace std;

int wynik,N,a,x;
int maxi = INT32_MAX;

int main(){
    ios_base::sync_with_stdio(0);
    cout.tie(0);
    cin.tie(0);
    cin>>N;
    for(int i=0; i^N; ++i){
        cin>>a;
        x = maxi;
        for()
    }


}