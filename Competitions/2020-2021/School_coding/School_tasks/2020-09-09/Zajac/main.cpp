#include <bits/stdc++.h>
#define ll long long
using namespace std;


int N;
ll k,a,b;
int tab [1001];
vector <pair<ll,ll> > v;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin>>N>>k;
    k*=k;
    for(int i=1; i<=N; ++i){
        cin>>a>>b;
        v.push_back({a,b});
    }
    tab[1]=1;
    for(int i=2; i<=N; ++i){
        
    }
    

}