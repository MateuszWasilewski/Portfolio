#include <bits/stdc++.h>
#define ld long double
using namespace std;
int n;
ld a,b;
ld wyn=0;
vector <pair<ld,ld> > v;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin>>n;
    for(int i=0;i<n;++i){
        cin>>a>>b;
        for(int z=1;z<v.size(); ++z){
            
        }
    }
}